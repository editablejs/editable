import { HrLocale } from './types'

const locale: HrLocale = {
  locale: 'en-US',
  hr: {
    toolbar: {
      defaultColor: 'Default color',
      color: 'Color',
      style: 'Style',
      width: 'Thickness',
    },
  },
}

export default locale
