import { InlineToolbarLocale } from './types'
import toolbarLocale from '../../locale'
const locale: InlineToolbarLocale = {
  locale: 'en-US',
  inlineToolbar: {
    colorPicker: toolbarLocale['en-US'].toolbar.colorPicker,
  },
}

export default locale
