import { SideToolbarLocale } from './types'
const locale: SideToolbarLocale = {
  locale: 'en-US',
  sideToolbar: {
    actionClick: 'Click',
    actionDrag: 'Drag',
    openMenu: 'open menu',
    dragDrop: 'move position',
  },
}

export default locale
