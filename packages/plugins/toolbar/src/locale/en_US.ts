import { ToolbarLocale } from './types'

const locale: ToolbarLocale = {
  locale: 'en-US',
  toolbar: {
    colorPicker: {
      recent: {
        title: 'Recently used',
        empty: 'None',
      },
      custom: {
        title: 'More colors',
      },
    },
  },
}

export default locale
